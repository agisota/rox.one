# Bad fixture (M.18 T253b): synopsis missing — validator must fail.
appId: com.rox.one
productName: ROX.ONE

linux:
  icon: resources/icon.png
  # T253: trust-boundary signing placeholder.
  category: Office
  # synopsis intentionally omitted — this is the bad fixture
  maintainer: "roxone <support@rox.one>"
  target:
    - target: AppImage
      arch:
        - x64
    - target: deb
      arch:
        - x64
    - target: rpm
      arch:
        - x64

deb:
  packageCategory: utils
  depends:
    - libnotify4
    - libnss3

rpm:
  packageCategory: Applications/Office
  depends:
    - libnotify
    - nss
