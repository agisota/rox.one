# Что нового в v0.8.11

- WhatsApp self-chat теперь симметрично фильтрует входящие сообщения.
- `call_llm` возвращает частичный вывод вместо пустой ошибки при лимите SDK turns.
- Pi-бэкенд учитывает отключение `Co-authored-by` в системном промпте.
- Follow-up цитаты отправляются агенту полностью, без прежней короткой обрезки.
- Вспомогательные функции follow-up вынесены в тестируемый модуль.
- Потоковый потребитель `queryLlm` вынесен и покрыт отдельными тестами.
- Pre-commit typecheck автоматически находит TypeScript workspaces.
- `electron:dev` собирает WhatsApp worker при запуске.
