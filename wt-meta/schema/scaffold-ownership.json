{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://rox.one/schemas/wt-meta/scaffold-ownership.json",
  "title": "ScaffoldOwnership",
  "description": "Authoritative ownership map for shared scaffold files across the parallel WT harness.",
  "type": "object",
  "additionalProperties": false,
  "required": ["owners"],
  "properties": {
    "owners": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["file", "owner"],
        "properties": {
          "file": {
            "type": "string",
            "minLength": 1,
            "description": "Path (relative to repo root) of the shared scaffold."
          },
          "owner": {
            "type": "string",
            "pattern": "^WT-\\d{2}$",
            "description": "Worktree ID that holds write authority. Format WT-NN."
          },
          "notes": {
            "type": "string",
            "description": "Free-form rationale; siblings request via scaffold_request in their wt-meta yaml."
          }
        }
      }
    }
  }
}
