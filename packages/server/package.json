{
  "name": "@rox-agent/server",
  "version": "0.8.12",
  "description": "Standalone headless ROX server (Bun runtime)",
  "type": "module",
  "main": "src/index.ts",
  "bin": {
    "rox-server": "src/index.ts"
  },
  "files": [
    "src/"
  ],
  "engines": {
    "bun": ">=1.0.0"
  },
  "author": {
    "name": "roxone",
    "email": "support@rox.one"
  },
  "license": "Apache-2.0",
  "homepage": "https://app.rox.one",
  "scripts": {
    "start": "bun run src/index.ts",
    "generate-token": "bun run src/index.ts --generate-token",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rox-agent/core": "workspace:*",
    "@rox-agent/messaging-gateway": "workspace:*",
    "@rox-agent/server-core": "workspace:*",
    "@rox-agent/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.8.2",
    "@types/node": "^22.0.0",
    "ws": "^8.16.0"
  }
}
