# T377 - R.11 test temp root ignore

Status: DONE
Phase: R.11 readiness hygiene
Ticket: docs/tickets/T377-r11-test-temp-root-ignore.md

## 1. Task summary

Ignore the two generated server-core observability test temp roots so R.11
worktree-clean checks are not polluted by expected local test output.

## 2. Repo context discovered

The primary worktree was otherwise synced with `origin/main`, but `git status`
reported:

```text
?? .tmp-test-file-audit-sink/
?? .tmp-test-host-audit-producer/
```

The paths are generated by tests:

- `packages/server-core/src/observability/__tests__/file-audit-sink.test.ts`
  defines `TEMP_ROOT = resolve(process.cwd(), '.tmp-test-file-audit-sink')`.
- `packages/server-core/src/observability/__tests__/host.test.ts` defines
  `TEMP_ROOT = resolve(process.cwd(), '.tmp-test-host-audit-producer')`.

Historical ticket/worklog evidence for T248 and T246b also documents these as
local test temp roots.

## 3. Files inspected

- `.gitignore`
- `packages/server-core/src/observability/__tests__/file-audit-sink.test.ts`
- `packages/server-core/src/observability/__tests__/host.test.ts`
- `docs/tickets/T248-file-audit-sink.md`
- `docs/worklog/T248-file-audit-sink.md`
- `docs/tickets/T246b-fileauditsink-host.md`
- `docs/worklog/T246b-fileauditsink-host.md`

## 4. Tests added first

No code test was added. The RED check was:

```bash
git check-ignore -v .tmp-test-file-audit-sink .tmp-test-host-audit-producer
```

Before this change, it exited non-zero and printed no matching ignore rule.

## 5. Expected failing test output

Expected RED before the `.gitignore` update:

```text
<no output>
```

with exit code `1`.

## 6. Implementation changes

- Added exact `.gitignore` entries for `.tmp-test-file-audit-sink/`.
- Added exact `.gitignore` entries for `.tmp-test-host-audit-producer/`.
- Did not delete existing generated temp contents.
- Did not widen the ignore rule to all `.tmp-*` paths.
- Did not run `git filter-repo`, create backup artifacts, or force-push refs.

## 7. Validation commands run

- `git check-ignore -v .tmp-test-file-audit-sink .tmp-test-host-audit-producer`
- `git status --short --branch`
- `bun run validate:docs`
- `bun run validate:rebrand`
- `git diff --check`

## 8. Passing test output summary

`git check-ignore -v` now reports the new `.gitignore` rules for both paths.

`git status --short --branch` no longer reports the generated temp roots as
untracked. Before commit, it only reports the staged `.gitignore`, T377 ticket,
and T377 worklog changes.

Documentation validation:

```text
[agent-contract] ok: 11 skills, 342 tickets, 7 required docs
[architecture-docs] ok: 4 docs, 10 subsystem headings
[sync-v2-design] validated docs/architecture/sync-v2-design.md
```

Rebrand validation:

```text
rebrand validation passed: no forbidden tokens outside the allowlist
```

Whitespace validation:

```text
git diff --check
```

exited 0 with no output.

## 9. Build output summary

No build was run for this ignore-only change. Runtime code and source behavior
are unchanged.

## 10. Remaining risks

R.11 remains blocked by active-goal, backup-tag, and offline-mirror
prerequisites. This ticket only removes a local generated-artifact cleanliness
problem; it does not unblock destructive history rewrite.

## 11. Acceptance criteria matrix

| Acceptance criterion | Status | Evidence |
| --- | --- | --- |
| Both generated test temp roots are ignored | Green | `git check-ignore -v` reports `.gitignore` matches for both paths |
| Existing generated temp contents are not deleted | Green | Only `.gitignore` and docs were edited |
| Primary worktree status no longer reports temp roots | Green | `git status --short --branch` excludes both `.tmp-test-*` roots |
| Documentation/rebrand validation remains green | Green | `validate:docs`, `validate:rebrand`, and `git diff --check` passed |
| Commit created | Green | Local Lore commit created for this ticket |
