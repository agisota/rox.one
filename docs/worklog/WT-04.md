# WT-04 — User + Identity data contract — worklog

**Branch:** `feat/contract-user-identity`
**Wave:** 0
**Base SHA:** `442126fac`
**Spec:** [`../superpowers/specs/2026-05-21-wt-04-contract-user-identity-design.md`](../superpowers/specs/2026-05-21-wt-04-contract-user-identity-design.md)
**Architecture:** [`../architecture/data-contract-user-identity-2026-05-21.md`](../architecture/data-contract-user-identity-2026-05-21.md)

## Status

Implemented. All 13 spec test cases (§13) covered by 42 passing unit tests
across 6 files. Postgres round-trip is the CI integration job (spec §16,
deferred from unit suite).

## Files landed

### `packages/shared/src/core/`

- `uuid-v7.ts` — generator, schema, `isUuidV7` guard, `DEFAULT_TENANT_ID`
- `user.ts` — `UserSchema`, `EmailSchema`, `LocaleSchema`, `TimezoneSchema`,
  `IsoUtcSchema`, `UserStatusSchema`, `UsernameSchema`, type exports
- `identity.ts` — `IdentitySchema`, `IdentityProviderSchema`,
  `IDENTITY_CLAIMS_MAX_BYTES`, type exports
- `index.ts` — barrel re-export
- `__tests__/uuid-v7.test.ts` — 6 tests (RFC 9562 conformance, sortability, perf)
- `__tests__/user.test.ts` — 6 tests (validation, defaults, status enum, perf)
- `__tests__/identity.test.ts` — 5 tests (claims size, provider enum, defaults)

### `packages/server-core/src/services/`

- `user-repository.ts` — `UserRepository`, `Result<T,E>`, `Ok`/`Err`,
  `hashEmailForAudit`, in-memory `UserStore` for unit tests
- `identity-repository.ts` — `IdentityRepository`, in-memory `IdentityStore`
- `__tests__/user-repository.test.ts` — 8 tests
- `__tests__/identity-repository.test.ts` — 7 tests

### `packages/server-core/src/persistence/migrations/`

- `2026-05-21-user-identity-up.sql` — creates `users` + `identities`,
  3 explicit indexes + 2 implicit unique-constraint indexes, all checks
- `2026-05-21-user-identity-down.sql` — idempotent reversal
- `../__tests__/user-identity-migration.test.ts` — 9 static-shape tests
  (table set parity, index drop parity, enum check parity, FK ON DELETE
  RESTRICT, transaction wrappers, drop order)

### Documentation

- `docs/architecture/data-contract-user-identity-2026-05-21.md`
- `docs/worklog/WT-04.md` (this file)

## Test mapping to spec §13

| Spec test                                                                              | Test file                          | Status |
|----------------------------------------------------------------------------------------|------------------------------------|--------|
| 1. UserSchema email reject                                                              | shared/core/__tests__/user.test    | pass   |
| 2. UserSchema default locale/timezone                                                   | shared/core/__tests__/user.test    | pass   |
| 3. UserSchema status enum                                                               | shared/core/__tests__/user.test    | pass   |
| 4. IdentitySchema claims >16KB                                                          | shared/core/__tests__/identity.test| pass   |
| 5. IdentitySchema unique (tenant, provider, externalId) via repo                        | server-core/services/__tests__/identity-repository.test | pass |
| 6. uuid-v7 sortable                                                                     | shared/core/__tests__/uuid-v7.test | pass   |
| 7. uuid-v7 RFC 9562 conformance                                                         | shared/core/__tests__/uuid-v7.test | pass   |
| 8. migrations up→down→up round-trip                                                     | persistence/__tests__/user-identity-migration.test (static) + CI Postgres (live) | partial (live = CI) |
| 9. UserRepository injects DEFAULT_TENANT_ID                                             | server-core/services/__tests__/user-repository.test    | pass   |
| 10. UserRepository duplicate email returns Err                                          | server-core/services/__tests__/user-repository.test    | pass   |
| 11. IdentityRepository findByProviderExternalId scoped                                  | server-core/services/__tests__/identity-repository.test | pass  |
| 12. IdentityRepository emits identity.linked                                            | server-core/services/__tests__/identity-repository.test | pass  |
| 13. soft-delete skips deleted_at_utc rows in list()                                     | server-core/services/__tests__/user-repository.test    | pass   |

## Acceptance criteria

| AC      | Evidence                                                                              | Status |
|---------|---------------------------------------------------------------------------------------|--------|
| AC-04.1 | Schemas exported from `@rox-one/shared/core`; typecheck:shared clean                  | pass   |
| AC-04.2 | up.sql + down.sql idempotent; migration test guards table/index parity                | pass (static); live = CI |
| AC-04.3 | Round-trip — static parity test passes; live Postgres test deferred to CI integration | pass (static); CI integration job pending |
| AC-04.4 | UUID v7 version=7, variant=10 verified in `uuid-v7.test.ts`                           | pass   |
| AC-04.5 | `UserRepository.create` with dup email returns `Err`, never throws                    | pass   |
| AC-04.6 | `findByProviderExternalId` tenant-scoped (cross-tenant returns separate identity)     | pass   |
| AC-04.7 | DEFAULT_TENANT_ID injected when tenant-v1 flag OFF                                    | pass   |
| AC-04.8 | 5 audit events emitted (user.created/updated/soft-deleted, identity.linked/unlinked)  | pass   |
| AC-04.9 | `hashEmailForAudit` returns 8-char hex; raw email never in audit payload              | pass   |
| AC-04.10| Zod parse <0.5ms p99 over 10k iterations; uuid-v7 <0.1ms                              | pass   |

## Scaffold-extension requests (to WT-00)

1. Add `"./core": "./src/core/index.ts"` to `packages/shared/package.json`
   exports map so downstream consumers can `import` via the published
   subpath at runtime. Schemas typecheck today via the `@rox-one/shared/*`
   path alias.
2. Register `rox.feature.contracts.user-v1` and
   `rox.feature.contracts.tenant-v1` in
   `packages/shared/src/feature-flags/registry.ts` (WT-07 owns).

Neither blocks WT-04 merge: schemas, repos, migrations are self-contained.

## Notes for downstream WTs

- `UserRepository` and `IdentityRepository` are constructible with an
  in-memory store today; swap in a Postgres adapter once
  `validate-server.yml` adds the testcontainers job.
- `Result<T, E>` from `./services/user-repository.ts` is re-used by
  `IdentityRepository`. Future repos in this package should import from
  the same source-of-truth file (or refactor to a shared `result.ts`
  in a follow-up).
- The audit shim is a callback (`AuditSink`); WT-08 will wire it to the
  real audit pipeline.
