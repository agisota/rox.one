# T346 - Missions IPC channel snapshot repair

Status: DONE
Phase: post-rebase full-suite repair
Ticket: docs/tickets/T346-missions-ipc-channel-snapshot-repair.md

## 1. Task summary

Repair the Electron shared IPC channel stability snapshot after current
`origin/main` added the four mission RPC channels.

## 2. Repo context discovered

`RPC_CHANNELS.missions` defines four dot-format wire strings:
`missions.create`, `missions.dispatchEvent`, `missions.get`, and
`missions.list`. The stability snapshot still expected 312 total channel
strings, while `RPC_CHANNELS` now exposes 316.

## 3. Files inspected

- `apps/electron/src/shared/__tests__/ipc-channels.test.ts`
- `packages/shared/src/protocol/channels.ts`
- `packages/shared/src/protocol/routing.ts`

## 4. Tests added first

No new test file was needed. The existing IPC channel stability test is the
failing regression gate.

## 5. Expected failing test output

`bun test apps/electron/src/shared/__tests__/ipc-channels.test.ts` failed with:

```text
Expected: 312
Received: 316
Received + "missions.create", "missions.dispatchEvent", "missions.get", "missions.list"
```

## 6. Implementation changes

- Added the four `missions.*` strings to
  `apps/electron/src/shared/__tests__/ipc-channels.test.ts`.
- Left mission runtime handlers and protocol channel definitions unchanged.

## 7. Validation commands run

- `bun test` (red)
- `bun test apps/electron/src/shared/__tests__/ipc-channels.test.ts` (red)
- `bun test apps/electron/src/shared/__tests__/ipc-channels.test.ts`
- `bun run typecheck`
- `bun run lint`
- `bun test`
- `bun run build`
- `git diff --check`

## 8. Passing test output summary

- `bun test apps/electron/src/shared/__tests__/ipc-channels.test.ts`:
  5 pass, 0 fail, 5 expect calls; channel count is now 316.
- `bun run typecheck`: exit 0.
- `bun run lint`: exit 0 with 3 existing React hook warnings and 0 errors.
- `bun test`: 5988 pass, 13 skip, 0 fail, 1 snapshot, 24614 expect calls.
- `git diff --check`: clean.

## 9. Build output summary

`bun run build` exited 0 after the IPC snapshot repair. Electron main,
preload, renderer, resources, and asset stages completed successfully.

## 10. Remaining risks

The file still says the snapshot is auto-generated by `scripts/ipc-inventory.ts`,
but that generator is not present in this checkout. This repair follows the
existing local pattern from T332 and updates the snapshot directly.

## 11. Acceptance criteria matrix

| Acceptance criterion | Status | Evidence |
| --- | --- | --- |
| IPC channel test fails before implementation for missions drift | Green | Full `bun test` and targeted IPC test failed with expected 312 vs received 316 count |
| Snapshot includes four `missions.*` strings | Green | Diff adds `missions.create`, `missions.dispatchEvent`, `missions.get`, `missions.list` |
| Targeted IPC channel test passes | Green | IPC channel test: 5 pass, 0 fail |
| Full `bun test` passes | Green | 5988 pass, 13 skip, 0 fail |
| Worklog complete | Green | Final validation evidence recorded |
| Commit created | Green | Atomic commit after validation |
