# T060 - Backlog Normalization Worklog

## 1. Task summary

Normalize ticket accounting for T042-T053 / T041 Experience Layer subtasks before starting upstream merge or runtime integration work.

## 2. Repo context discovered

- Branch is `main`.
- `origin/main...main` was `0 0` before T060 edits.
- The only pre-existing dirty tracked file was `events.jsonl`, a runtime log generated by app launch.
- `docs/tickets` had 48 canonical ticket files plus `TEMPLATE.md`.
- `docs/worklog` had implementation worklogs for T042-T053, but matching canonical ticket files were absent.
- `scripts/validate-agent-contract.ts` only counted ticket files and did not check ticket/worklog accounting.
- Current session is not inside tmux, so `$team` was not launched; native read-only subagents were used for discovery.

## 3. Files inspected

- `AGENTS.md`
- `package.json`
- `scripts/validate-agent-contract.ts`
- `docs/tickets/`
- `docs/worklog/`
- `docs/tickets/T041-experience-layer-system.md`
- `docs/prompts/experience-layer-autopilot.md`
- `docs/product/experience-layer-system-prd.md`
- `.swarm/inventory.md`
- `.swarm/backlog-status.md`
- `plan.md`

## 4. Tests added first

Extended `scripts/validate-agent-contract.ts` before creating the missing ticket files.

The new validation checks:

- every worklog id must have a canonical ticket id;
- every `DONE` ticket id must have at least one matching worklog id;
- every canonical ticket must include a `Status:` line.

## 5. Expected failing test output

`bun run validate:agent-contract` failed before canonical ticket files were added:

```text
[agent-contract] worklog ids without canonical ticket files: T042, T043, T044, T045, T046, T047, T048, T049, T050, T051, T052, T053
```

This was the expected red phase for T060.

## 6. Implementation changes

- Added canonical DONE ticket files for `T042` through `T053`.
- Added `docs/tickets/T060-backlog-normalization.md`.
- Added this worklog.
- Extended `scripts/validate-agent-contract.ts` with ticket/worklog accounting checks.
- Updated `.swarm/inventory.md` and `.swarm/backlog-status.md` to describe the normalized state.

No product/runtime code was changed.

## 7. Validation commands run

- `bun run validate:agent-contract`
- `bun run validate:docs`
- `git diff --check`

## 8. Passing test output summary

- `bun run validate:agent-contract`: `[agent-contract] ok: 11 skills, 61 tickets, 7 required docs`
- `bun run validate:docs`: agent contract, architecture docs, and sync V2 design validation passed.
- `git diff --check`: no whitespace errors.

## 9. Build output summary

No runtime build is required for T060 because the change is limited to docs and validation tooling.

## 10. Remaining risks

- T060 proves accounting consistency by ticket/worklog id, not a full semantic audit of every historical worklog.
- T061 must still verify upstream tags live because upstream can move.
- The pre-existing `events.jsonl` runtime log remains outside the T060 commit scope.

## 11. Acceptance criteria matrix

| Criteria | Status | Evidence |
|---|---:|---|
| Every DONE ticket has matching worklog | DONE | `bun run validate:agent-contract` |
| Every T041 subtask explicitly represented | DONE | `docs/tickets/T042-*.md` through `docs/tickets/T053-*.md` |
| Docs validator catches missing ticket/worklog aliases | DONE | Red phase from `bun run validate:agent-contract` |
| No product/runtime code changed | DONE | T060 touches docs and validation only |
| Worklog complete | DONE | This worklog |
