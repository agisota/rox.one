# T332 - Roles IPC channel snapshot repair

Status: DONE
Phase: M.2 validation repair
Ticket: docs/tickets/T332-roles-ipc-channel-snapshot-repair.md

## 1. Task summary

Repair the Electron shared IPC channel stability snapshot after PR #75 added
the four roles RPC channels.

## 2. Repo context discovered

`bun test` failed in `apps/electron/src/shared/__tests__/ipc-channels.test.ts`
because `RPC_CHANNELS` now has 312 strings, while the test snapshot expected
308. The four received-only values were `roles.create`, `roles.grant`,
`roles.list`, and `roles.revoke`.

## 3. Files inspected

- `apps/electron/src/shared/__tests__/ipc-channels.test.ts`
- `packages/shared/src/protocol/channels.ts`
- `packages/shared/src/protocol/routing.ts`
- `packages/server-core/src/handlers/rpc/roles.ts`
- `packages/server-core/src/handlers/rpc/__tests__/roles.test.ts`
- `docs/tickets/T227-rbac-admin-rpc.md`
- `docs/worklog/T227-rbac-admin-rpc.md`

## 4. Tests added first

No new test file was needed. The existing IPC channel stability test is the
failing regression gate.

## 5. Expected failing test output

Full `bun test` failed with:

```text
Expected: 308
Received: 312
Received + "roles.create", "roles.grant", "roles.list", "roles.revoke"
```

## 6. Implementation changes

- Added `roles.create`, `roles.grant`, `roles.list`, and `roles.revoke` to
  `apps/electron/src/shared/__tests__/ipc-channels.test.ts`.
- Updated T227 ticket/worklog channel-name wording to use the actual
  `roles.*` dot-format strings from `RPC_CHANNELS.roles`.

## 7. Validation commands run

- `bun test` (red)
- `bun test apps/electron/src/shared/__tests__/ipc-channels.test.ts`
- `bun test packages/server-core/src/handlers/rpc/__tests__/roles.test.ts`
- `bun run validate:docs`
- `git diff --check`
- `bun test`
- `bun run build`

## 8. Passing test output summary

- `bun test apps/electron/src/shared/__tests__/ipc-channels.test.ts`: 5 pass,
  0 fail, 5 expects; channel count is now 312.
- `bun test packages/server-core/src/handlers/rpc/__tests__/roles.test.ts`:
  30 pass, 0 fail, 34 expects.
- `bun run validate:docs`: agent-contract, architecture-docs, and
  sync-v2-design validators passed; agent contract reported 11 skills,
  240 tickets, and 7 required docs.
- `git diff --check`: clean.
- `bun test`: 5258 pass, 13 skip, 0 fail, 1 snapshot, 13419 expects across
  5271 tests in 476 files.

## 9. Build output summary

`bun run build` completed successfully after this repair. Electron main,
preload, renderer, resources, and assets builds completed; the only observed
output of note was the existing Vite large-chunk warning.

## 10. Remaining risks

The file says the snapshot is auto-generated by `scripts/ipc-inventory.ts`, but
that script is not present in this checkout. This repair updates the snapshot
directly to match the current `RPC_CHANNELS` object.

## 11. Acceptance criteria matrix

- [x] Full `bun test` fails before implementation for the expected roles channel snapshot drift.
- [x] IPC channel stability snapshot includes the four roles channels.
- [x] T227 docs name the actual `roles.*` wire strings.
- [x] Targeted IPC/RBAC tests pass.
- [x] Full `bun test` passes.
- [x] Worklog complete.
