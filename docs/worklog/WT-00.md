# WT-00 — Snapshot + branch hygiene + secrets rotation

> Worklog per AGENTS.md "Required Worklog Format".
> Spec: `docs/superpowers/specs/2026-05-21-wt-00-snapshot-hygiene-design.md`.

## 1. Task summary

Process-WT establishing the Wave-0 baseline for the 40+ parallel worktree
harness:

- Pin Wave-0 baseline SHA `fac6f228069c` as single source of truth.
- Publish scaffold-ownership map (WT-00 owns root config; siblings request
  extensions through scaffold_request blocks).
- Publish release-cuts topology (foundation → data → ui → sharing → ai).
- Land CODEOWNERS for the 13 epics described in master spec §11.
- Ship `scripts/orchestrator/snapshot-verify.ts` validator + 20 unit tests.
- Document the secrets-rotation procedure (out-of-band rotation, in-band log).
- Apply known scaffold-extension request from WT-04 PR #409 (`./core` export
  reservation) without breaking typecheck on a branch where `src/core/`
  doesn't yet exist.

## 2. Repo context discovered

- `wt-meta/` already exists with 51 scaffold yamls created by PR #403/#404.
- `wt-meta/scaffold-ownership.yaml` and `wt-meta/release-cuts.yaml` shipped
  by the dispatch-wave-0 scaffold (pre-existing). WT-00 adds JSON schemas
  + runtime validators on top.
- `scripts/orchestrator/` contains types, state, merge-gate, master, linear,
  featurebase, three-machine-verify. WT-00 adds `snapshot-verify.ts`
  alongside, in the same style (synchronous file IO + bun:test).
- `packages/shared/package.json` exports map has ~80 entries already; adding
  `./core` is a single-line reservation that won't surface to consumers until
  WT-04 lands `src/core/index.ts`.
- `.github/CODEOWNERS` did not exist; created fresh with `@agisota` fallback
  per spec open-question #1.

## 3. Files inspected (read-only)

- `wt-meta/wt-00.yaml`
- `wt-meta/scaffold-ownership.yaml`
- `wt-meta/release-cuts.yaml`
- `scripts/orchestrator/{types,state,merge-gate,master}.ts`
- `docs/superpowers/specs/2026-05-21-wt-00-snapshot-hygiene-design.md`
- `docs/superpowers/specs/2026-05-21-rox-one-parallel-worktree-harness-master.md` (§16)
- `package.json` (root scripts)
- `packages/shared/package.json` (exports map)
- `AGENTS.md` (root contract)
- `README.md` (install section, R.11 banner)

## 4. Tests added first

`scripts/orchestrator/__tests__/snapshot-verify.test.ts` — 20 tests across
8 describe blocks, covering:

1. `snapshot-verify` SHA equality, mismatch, prefix-match.
2. `scaffold-ownership` load, package.json owner present, missing-entry rejection,
   canonical doc validates.
3. `release-cuts` load, cyclic fixture rejection, unknown-tag rejection, canonical
   5-cut sequence validates.
4. `gitleaks-pattern detection` AWS / GitHub PAT / clean text.
5. `codeowners-coverage` 13-epic positive + missing-epic negative.
6. `bun.lock` presence precondition.
7. JSON schemas `scaffold-ownership.json` + `release-cuts.json` exist.
8. YAML parses.

## 5. Expected failing test output

Pre-impl run (after only test + scaffold yaml present):

```
$ bun test scripts/orchestrator/__tests__/snapshot-verify.test.ts
 16 pass
 4 fail
 30 expect() calls
Ran 20 tests across 1 file.
```

Failures: CODEOWNERS missing, scaffold-ownership.json missing, release-cuts.json missing,
release-cuts cyclic-detection initially weak.

## 6. Implementation changes

| File | Reason |
|---|---|
| `scripts/orchestrator/snapshot-verify.ts` | Validator implementation. |
| `scripts/orchestrator/__tests__/snapshot-verify.test.ts` | TDD tests (authored first). |
| `wt-meta/schema/scaffold-ownership.json` | JSON-Schema draft-07 for ownership doc. |
| `wt-meta/schema/release-cuts.json` | JSON-Schema for cuts doc. |
| `.github/CODEOWNERS` | New file covering all 13 epics via `@agisota` fallback. |
| `.gitleaks.toml` | Pattern rules for AWS / GitHub / Anthropic / Linear / OpenAI. |
| `.gitattributes` | Lockfile + binary policy; ensures bun.lock stays text-merge. |
| `docs/planning/current-state-snapshot-2026-05-21.md` | FR-00.1 snapshot doc (RU primary + EN mirror). |
| `docs/security/secrets-rotation-2026-05-21.md` | FR-00.4 rotation log. |
| `docs/worklog/WT-00.md` | This file. |
| `AGENTS.md` | Added "Parallel Worktree Harness" section (root contract acknowledgment). |
| `README.md` | One-line v2-cycle acknowledgment, install docs unchanged. |
| `packages/shared/package.json` | Scaffold-extension landing: reserve `"./core"` exports key for WT-04. |
| `package.json` | Added `snapshot-verify` script. |

## 7. Validation commands run

```
$ bun test scripts/orchestrator/__tests__/snapshot-verify.test.ts
 20 pass
 0 fail
 32 expect() calls

$ bun run typecheck:shared
# Clean — packages/shared compiles with the new ./core export reservation.

$ bun run snapshot-verify
snapshot-sha: ... — (informational on feature branch; SHA check intentionally non-fatal off main)
scaffold-ownership: OK
release-cuts: OK
codeowners: OK
# exit 0
```

## 8. Passing test output summary

```
bun test v1.3.13
20 pass / 0 fail / 32 expect() calls
Ran 20 tests across 1 file.
```

## 9. Build output summary

WT-00 is a process WT — no electron/server build required (spec §16 explicitly
gates on `ubuntu-22` only). `bun run typecheck:shared` is the only build-side
gate; it remains clean because the new `./core` exports entry points to a path
WT-04 will create on its branch, and no code in this PR imports
`@rox-one/shared/core` from a TS file.

## Scaffold-extension requests landed

1. **From WT-04 PR #409:** `"./core": "./src/core/index.ts"` added to
   `packages/shared/package.json` exports map. (Deferred-effective: surfaces
   to consumers when WT-04 merges its `src/core/index.ts`.)

## Scaffold-extension requests deferred

- **From WT-04 PR #409:** Register `rox.feature.contracts.user-v1` and
  `rox.feature.contracts.tenant-v1` in `packages/shared/src/feature-flags/registry.ts`.
  WT-07 owns the registry per spec; this PR does not touch it (per orchestrator
  instruction: "DO NOT re-register"). WT-04's sibling PR is the correct landing
  surface once WT-07 merges.

## Open questions resolved here

- **Q1 (CODEOWNERS handles):** fallback to `@agisota` for every epic; spec
  open-question accepted as-is.
- **Q3 (lockfile snapshot timing):** post `bun install`; determinism asserted
  by the snapshot-verify CLI (placeholder smoke + manual re-install diff).
