# Agent Workbench Tickets

This directory is the task backlog for turning the Rox Agents fork into Agent Workbench Suite.

Rules:

1. One task per ticket file.
2. Each task gets a matching worklog in `docs/worklog/`.
3. Tests or validation checks are written before implementation.
4. A task is complete only when its acceptance matrix is green and a commit exists.
