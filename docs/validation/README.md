# Validation

This directory records validation commands and project quality gates for the Agent Workbench fork.

The first gate is `bun run validate:agent-contract`, which checks the repo-local agent operating contract, skill metadata, tickets, and worklog scaffolding.
